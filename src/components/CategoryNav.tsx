'use client'

import { motion } from 'framer-motion'

const categories = [
  // All categories removed as requested
]

export default function CategoryNav() {
  // All categories removed as requested
  return null
}
